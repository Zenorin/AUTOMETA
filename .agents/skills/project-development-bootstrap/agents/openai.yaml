interface:
  display_name: "Project Development Bootstrap"
  short_description: "Create and validate AUTOMETA repo bootstrap, workspace, env template, planning docs, and shared build scripts without overriding module routing."
  default_prompt: "$project-development-bootstrap"

policy:
  allow_implicit_invocation: true
